<form method="post" action="{{ path('antiquity_days_delete', {'id': antiquity_day.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ antiquity_day.id) }}">
    <button class="btn">Delete</button>
</form>

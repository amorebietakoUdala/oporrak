{% extends 'base.html.twig' %}

{% block title %}{{ 'modal.title.event' | trans  }}{% endblock %}

{% block body %}
<div class="container-fluid mt-4">
    <h1>{{ 'modal.title.event' | trans  }}</h1>

    {{ include('event/_form.html.twig') }}

    <a href="{{ path('calendar') }}">back to list</a>
</div>
{% endblock %}

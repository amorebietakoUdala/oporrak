{% extends 'base.html.twig' %}

{% block title %}{{ 'title.workcalendar.edit' | trans | capitalize }}{% endblock %}

{% block body %}
    <h1>{{ 'title.workcalendar.edit' | trans | capitalize }}</h1>

    {{ include('department/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('department_index') }}">back to list</a>

    {{ include('department/_delete_form.html.twig') }}
{% endblock %}

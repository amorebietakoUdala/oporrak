<form method="post" action="{{ path('additional_vacation_days_delete', {'id': additional_vacation_day.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ additional_vacation_day.id) }}">
    <button class="btn">Delete</button>
</form>

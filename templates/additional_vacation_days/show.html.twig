{% extends 'base.html.twig' %}

{% block title %}{{'title.additionalVacationDaysIndex' | trans }}{% endblock %}

{% block body %}
    <h1>{{'title.additionalVacationDaysIndex' | trans }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ additional_vacation_days.id }}</td>
            </tr>
            <tr>
                <th>YearsWorked</th>
                <td>{{ additional_vacation_days.yearsWorked }}</td>
            </tr>
            <tr>
                <th>VacationDays</th>
                <td>{{ additional_vacation_days.vacationDays }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('additional_vacation_days_index') }}">back to list</a>

    <a href="{{ path('additional_vacation_days_edit', {'id': antiquity_day.id}) }}">edit</a>

    {{ include('additional_vacation_days/_delete_form.html.twig') }}
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}{{ 'title.department.new' | trans | capitalize }}{% endblock %}

{% block body %}
    <h1>{{ 'title.department.new' | trans | capitalize }}</h1>

    {{ include('department/_form.html.twig') }}

    <a href="{{ path('department_index') }}">back to list</a>
{% endblock %}
